# $Id$

# This is just includes Makefile.mk where all the work is done.  This
# allows autoconfigure to overwrite this file while retaining the
# functionality of this Makefile with
#
#   make -f Makefile.mk.

include Makefile.mk
